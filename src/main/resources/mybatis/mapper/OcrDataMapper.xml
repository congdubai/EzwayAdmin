<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.infoplus.ezway.EzwayAdmin.mapper.OcrDataMapper">

    <select id="findByTransId" parameterType="string" resultType="com.infoplus.ezway.EzwayAdmin.dto.CommonDTO">
        SELECT trans_id AS transId,
        front_image AS image1,
        back_image AS image2,
        result_code AS result
        FROM tbl_ocr_data
        WHERE trans_id = #{transId}
        AND is_latest = 'Y'
    </select>

    <select id="findByTransId2" parameterType="string" resultType="com.infoplus.ezway.EzwayAdmin.dto.orc.OcrDataDto">
        SELECT idcard_type AS idcardType,
        address,
        birthday,
        doctype,
        expiration,
        id_no        AS idNo,
        issue_date   AS issueDate,
        full_name    AS fullName,
        country,
        sex,
        create_date  AS createDate,
        update_date  AS updateDate
        FROM tbl_ocr_data
        WHERE trans_id = #{transId}
        AND is_latest = 'Y'
    </select>

</mapper>


